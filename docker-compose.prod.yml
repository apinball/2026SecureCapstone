services:
  proxy-server:
    build: ./nginx
    container_name: pqc-proxy-prod
    ports:
      - "80:80"
      - "443:443"
    restart: always
    volumes:
      - ./nginx/nginx.conf:/opt/nginx/nginx-conf/nginx.conf
    depends_on:
      - backend-api
    networks:
      - pqc-network

  backend-api:
    build: ./backend
    container_name: service-backend-prod
    restart: always
    environment:
      - ENV=production
    networks:
      - pqc-network

  test-client:
    build: ./tester
    container_name: tls-tester-prod
    restart: on-failure
    command: tail -f /dev/null
    networks:
      - pqc-network

networks:
  pqc-network:
    driver: bridge